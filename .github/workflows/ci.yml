# Starter repo CI: Day 4 ordering (unit → integration) + Day 5 (REST Assured Failsafe + Newman).
# Optional repo variable: API_BASE_URL (Settings → Secrets and variables → Actions → Variables).
# Leave unset in GitHub to use WireMock inside UserApiIT (no external API for the Java suite).

name: Starter — unit, API (REST Assured), integration, Newman

on:
  push:
    branches:
      - main
      - dev
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  unit-tests:
    name: Unit tests (exclude integration)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: maven

      - name: Cache Maven dependencies
        uses: actions/cache@v4
        with:
          path: ~/.m2
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}

      - name: Run unit tests
        run: |
          chmod +x mvnw
          ./mvnw -B test -DexcludedGroups=integration

      - name: Upload Surefire XML (always)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: surefire-unit-${{ github.run_id }}
          path: target/surefire-reports/
          if-no-files-found: warn

  docker-compose-day6:
    name: Day 6 — Docker Compose (Postgres + test-runner)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build images and run tests
        env:
          DB_PASS: testpass
        run: docker compose -f docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from test-runner

      - name: Copy Surefire reports from test-runner
        if: always()
        run: |
          mkdir -p surefire-docker-day6
          docker compose -f docker-compose.test.yml cp test-runner:/app/target/surefire-reports ./surefire-docker-day6/ || true

      - name: Tear down containers and volumes
        if: always()
        run: docker compose -f docker-compose.test.yml down -v

      - name: Upload Surefire XML (Day 6 container run)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: surefire-docker-day6-${{ github.run_id }}
          path: surefire-docker-day6/
          if-no-files-found: warn

  api-tests:
    name: REST Assured (Failsafe, groups=api)
    needs: unit-tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: maven

      - name: Run API integration tests
        env:
          API_BASE_URL: ${{ vars.API_BASE_URL }}
        run: |
          chmod +x mvnw
          ./mvnw -B verify -Papi-only

      - name: Upload Failsafe reports (always)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: failsafe-api-${{ github.run_id }}
          path: target/failsafe-reports/
          if-no-files-found: warn

  integration-tests:
    name: Integration tests
    needs: api-tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: maven

      - name: Run integration-tagged tests
        run: |
          chmod +x mvnw
          ./mvnw -B test -Dgroups=integration

      - name: Upload Surefire XML (always)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: surefire-integration-${{ github.run_id }}
          path: target/surefire-reports/
          if-no-files-found: warn

  newman-api:
    name: Newman (Postman collection)
    needs: api-tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Install Newman (project devDependency)
        run: npm install

      - name: Run collection
        run: npm run newman

      - name: Upload Newman JUnit (always)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: newman-junit-${{ github.run_id }}
          path: postman/results/newman-junit.xml
          if-no-files-found: warn
